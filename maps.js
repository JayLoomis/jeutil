/*
The Maps library is a lightweight library for dealing with tilemaps.
In its present form, this library can manage grid maps with rectangular tiles only.
*/

class Map {
  // Constructor takes a name, a tileset, and optionally, a mapfile.
}
